App.Views.ContainerView = Backbone.View.extend({
  
  template: JST["container/container_view"],
  
  initialize: function() {
    // Initialize Current Tasks View
    // Initialize Today Tasks View
  }
  
});